# Notebook Alanı

Bu klasör isteğe bağlı araştırma ve keşif çalışmaları için ayrılmıştır.

Notebook kullanımı zorunlu değildir. Üretim akışı `app/ml/train_model.py` dosyasındaki tekrar çalıştırılabilir eğitim pipeline'ı üzerinden yürütülür.

